# Secrets & local artifacts — never commit
.env
*.env
!*.env.example
agent/results.local.json
results.local.json
agent/benchmark_results.local.json

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/

# Local model weights — downloaded/bundled into the image, never into git (GBs)
agent/models/
models/
*.gguf

# Slides — Manim build scratch (partial movies, LaTeX intermediates, preview PNGs).
# The deliverables are presentation/final.mp4 and presentation/deck.pdf; the six
# slides/<Scene>.json files and their clips under slides/files/ drive `manim-slides
# present|convert`. Everything under media/ is regenerated by `manim render`.
media/
