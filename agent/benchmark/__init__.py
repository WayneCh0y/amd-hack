"""Offline benchmark harness (dev-only, excluded from the Docker image).

Measures each allowed model's accuracy and token cost per category so the
routing/model strategy can be chosen from data on launch day rather than guessed.
"""
